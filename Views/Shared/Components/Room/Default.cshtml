@model IEnumerable<Chat>

<div class="side-menu">
    @foreach (var room in Model)
    {
        <a class="room-button" asp-action="Chat" asp-route-room="@room.Name">@room.Name</a>
    }
    <a id="create-room-btn" class="room-button" href="#">
        <strong>+</strong>
    </a>
</div>
